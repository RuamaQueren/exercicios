/*Crie um algoritmo que escreva todos os números pares de 1 a 100*/

for (pares = 1; pares <=100; pares ++) {
    if (pares%2==0) {
        console.log (pares)
    }
}

/*Crie um algoritmo que escreva todos os múltiplos de 5 de 1 a 50*/

for (multiplos = 1; multiplos <=50; multiplos ++) {
    if (multiplos%5==0) {
        console.log (multiplos)
    }
}

/*Crie um algoritmo que escreva a soma de todos os números entre dois números específicos*/

var numero = 1;
var soma = 0;
    while (numero <=50) {
        console.log (soma)
        numero +=1
        soma += numero;
    }

/*Crie um algoritmo que escreva a soma de todos os números de 1 a 30*/

var numero = 1;
var soma = 0;
    while (numero <=30) {
        console.log (soma)
        numero +=1
        soma += numero;
    }